Approve Expert
    - Get email/userID from webpage
    - PATCH /api/experts/{userID} 

Expert Signup
    - get email & password from web form
    - validate
        - email not taken GET /api/users?email={email}
        - pass length
        - pass contains num
        - pass contains uppercase
        - pass contains lowercase
    - if no error: POST /api/users & POST /api/experts
    - else: visual to show errors

Get Resources
    - /api/expertresources?userID={userID}

Get Results
    - /api/experts?adminVerified={},studentOnline={}, etc...


Get Status - remain same?
Login - remain same?
Logout - remain same?

Remove Admin
    - get email from webpage
    - PATCH /api/users/{userID}

Update Expert
    - PATCH /api/experts/{userID}

Update Resource
    - where resourceID present: PATCH /api/expertresource/{resourceID}
    - else: POST /api/expertresource

Expert Signup
    - get email & password from web form
    - validate
        - email not taken GET /api/users?email={email}
        - pass length
        - pass contains num
        - pass contains uppercase
        - pass contains lowercase
    - if no error: POST /api/users
    - else: visual to show errors